import React from 'react';
import * as d3 from 'd3';

const MedianLine = ({
    data,
    value,
    width,
    x, 
    y,
    bottomMargin,
    median
}) => {
    return(
        <g>
            
        </g>
    );
};

export default MedianLine;